{
  "name": "mylibrary",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "bun test ./tests/*.test.js && npx playwright test",
    "test:unit": "bun test ./tests/*.test.js",
    "test:integration": "npx playwright test tests/sync.spec.js",
    "test:browser": "npx playwright test tests/browser.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
